```markdown
# SGN 文档索引与功能决策矩阵 (sgn_index_matrix)

> **版本**: v2.1 (数学澄清版)  
> **日期**: 2026-06-09  
> **性质**: SGN 技术栈总索引与决策指南  
> **核心原则**: 本文档是进入 SGN 规范体系的第一入口。任何开发者应先查阅本索引，再定位到具体核心文件。

---

## 0. 版本变更记录

| 版本 | 日期 | 变更摘要 |
|------|------|----------|
| v2.0 | 2026-05-27 | 初始权威重构 |
| **v2.1** | 2026-06-09 | **数学澄清**：修正 `stored_level` 语义重载，明确 `GLOBAL_LEVEL` 唯一决策地位；增加投影/嵌入算子数学定义；补充合体模式下超度量球撕裂的边界说明；声明饱和加法的半环性质和补码的群结构；优化跨档位示例，消除歧义。所有修改不改变 ABI。 |

---

## 1. 核心文件架构（9 份权威地基）

```

sgn_spec/
├── sgn_math_core.md          ← 数学地基：超度量距离、强三角不等式、分离模式几何 (v2.1)
├── sgn_hc_ops.md             ← 运算地基：比较、加法、减法、软阈值、溢出语义 (v2.1)
├── sgn_trie_core.md          ← 索引地基：Trie 节点、查找、插入、遍历、收集 (v2.1)
├── sgn_config_guide.md       ← 配置地基：三种模式、精度档位、存储格式、内存互斥 (v2.1)
├── sgn_engine_core.md        ← 引擎地基：WTA、软阈值衰减、LRU、形态学、排序网络 (v2.1)
├── sgn_storage_core.md       ← 存储地基：RLE、Merkle、RS、校验和、压缩栈 (v2.0)
├── sgn_network_core.md       ← 网络地基：UART、GPIO、Lamport、Quorum、看门狗 (v2.0)
├── sgn_reliability_core.md   ← 可靠地基：TMR、校验和、RS、Merkle 四级链路 (v2.0)
└── sgn_index_matrix.md       ← 本文件：总索引、决策矩阵、原始文档映射 (v2.1)

```

**使用规则**:
- 9 份核心文件构成 **ABI 契约**，所有上层实现必须调用其中定义的接口
- 禁止在扩展文档中重复实现核心代码，禁止重复证明数学定理
- 扩展文档（如 2-adic 平方根、布朗运动等）仅保留业务逻辑，基础运算引用核心文件

---

## 2. 功能选择决策矩阵（唯一权威）

### 2.1 按场景查表

| 场景 | 首选方案 | 调用接口 | 备选方案 | 禁止选择 |
|------|---------|---------|---------|---------|
| **模板检索**（已知签名） | Trie 前缀剪枝 | `sgn_trie_collect_candidates()` [sgn_trie_core §5.2] | — | 霍夫曼码（仅传输压缩） |
| **模板存在预筛** | 计数布隆过滤器 | `sgn_cbf_query()` [扩展] | — | WTA（仅精确竞争） |
| **模板精确竞争** | WTA Top-K | `sgn_wta_compete()` [sgn_engine_core §1] | — | 线性扫描（N>256 时不可行） |
| **全局衰减** | 软阈值 | `sgn_soft_decay_all()` [sgn_engine_core §2] | — | LRU（仅淘汰决策） |
| **淘汰决策** | LRU 字典序最小 | `sgn_lru_find_evict()` [sgn_engine_core §3] | — | 硬截断（信息湮灭） |
| **在线聚类** | Fréchet 均值 | `sgn_frechet_mean()` [扩展] | — | 形态学（仅离线清理） |
| **离线清理** | 形态学开运算 | `sgn_open()` [sgn_engine_core §5] | — | Fréchet（仅在线中心） |
| **本地超时** | 看门狗定时器 | `sgn_wdt_start()` [sgn_network_core §5] | — | Lamport（仅分布式） |
| **分布式因果排序** | Lamport 逻辑时钟 | `sgn_lamport_update()` [sgn_network_core §3] | — | 看门狗（仅本地） |
| **多节点数值共识** | Quorum 中位数 | `sgn_median_layerwise()` [sgn_network_core §4] | — | TMR（仅单节点副本） |
| **单节点实时容错** | TMR 多数投票 | `sgn_tmr_vote()` [sgn_reliability_core §2] | — | Quorum（仅多节点） |
| **RAM 实时嗅探** | 校验和增量 | `sgn_checksum_delta()` [sgn_reliability_core §3] | — | Merkle（仅 Flash 审计） |
| **Flash 持久审计** | Merkle 树 | `sgn_merkle_update_leaf()` [sgn_storage_core §2] | — | 校验和（仅 RAM） |
| **传感器输入** | GPIO 并行 | `sgn_hc8_parallel_in()` [sgn_network_core §2] | — | UART（板内延迟过高） |
| **节点间同步** | UART 帧 | `sgn_uart_pack_frame()` [sgn_network_core §1] | — | GPIO（距离受限） |
| **模型压缩** | RLE + 组头 | `sgn_hc_rle_encode()` [sgn_storage_core §1] | 霍夫曼码 | 裸二进制（无压缩） |
| **完整性校验** | RS-LFSR | `rs86_encode_lfsr()` [sgn_storage_core §3] | — | RS 查表（Flash 不可行） |

### 2.2 按模式查表

| 数值语义 | 推荐模式 | 运算规则 | 溢出处理 | 引用 |
|---------|---------|---------|---------|------|
| 像素、速度、置信度 | **仅 HC** | 饱和/回绕加法 | int_part clamp 255 | [sgn_config_guide §1] |
| 模板签名、特征向量 | **仅 HC** | 字典序比较 | 无溢出（范围内） | [sgn_config_guide §1] |
| 训练步数、轮次 | **仅 level** | 普通整数加法 | 无上限 | [sgn_config_guide §1] |
| 时间戳（单次判断） | **HC16 单独** | 减法借位 | sec 模 65536 | [sgn_config_guide §6] |
| 系统总时长、累计量 | **合体** | 完整进位 | 向 level 进位 | [sgn_config_guide §1] |

---

## 3. 原始文档到核心文件映射

### 3.1 已合并到核心文件的原始文档

| 原始文档 | 合并到核心文件 | 保留内容 | 删除内容 |
|---------|--------------|---------|---------|
| 数值体系的超度量性质 v1.1 | sgn_math_core | 证明、定义、案例 | 无 |
| HC与level交叉运作机制 v1.1 | sgn_math_core + sgn_hc_ops | 进位规则 | 重复证明 |
| 视觉结构与实际结构 v2.1 | sgn_math_core | 比喻、直觉 | 重复定义 |
| HC_level分离指南 | sgn_config_guide | 速查表、转换规则 | 数学推导 |
| 时间变量处理分工 v1.1 | sgn_config_guide | 时间戳分工 | 重复溢出语义 |
| 合体存储精度档位 v1.0 | sgn_config_guide | 档位表、文件格式 | 重复模式定义 |
| WTA竞争算子 v1.0 | sgn_engine_core | TopK 排序逻辑 | Trie 实现代码 |
| 软阈值稀疏诱导 v1.0 | sgn_engine_core + sgn_hc_ops | 休眠区模型 | 减法实现代码 |
| LRU计数器 v1.0 | sgn_engine_core + sgn_hc_ops | 淘汰策略 | 比较实现代码 |
| 形态学膨胀腐蚀 v1.0 | sgn_engine_core + sgn_trie_core | 开/闭运算语义 | Trie 遍历代码 |
| 字典序排序网络 v1.0 | sgn_engine_core + sgn_hc_ops | 双调网络深度 | 比较实现代码 |
| 超度量哈希与Trie索引 v1.0 | sgn_trie_core | 保前缀哈希 | 节点结构代码 |
| DFA状态编码 v1.0 | sgn_trie_core | 状态转移语义 | 子节点查找代码 |
| 霍夫曼前缀码 v1.0 | sgn_trie_core | 截断策略 | Trie 建树代码 |
| 游程编码与熵压缩 v1.0 | sgn_storage_core | 压缩率数据 | 无 |
| Merkle树 v1.0 | sgn_storage_core + sgn_reliability_core | 增量更新 | 加法实现代码 |
| 校验和增量更新 v1.0 | sgn_storage_core + sgn_reliability_core | 增量公式 | 无 |
| ReedSolomon纠错 v1.0 | sgn_storage_core + sgn_reliability_core | LFSR 算法 | 查表法（删除） |
| TMR三模冗余 v1.0 | sgn_reliability_core | 投票策略 | 无 |
| UART串行通信 v1.0 | sgn_network_core | 帧格式 | 无 |
| I/O引脚并行编码 v1.0 | sgn_network_core | 映射规则 | 无 |
| Lamport时钟同步 v1.0 | sgn_network_core | 更新规则 | 重复时间戳定义 |
| Quorum共识 v1.0 | sgn_network_core | 中位数定理 | 重复比较定义 |
| 看门狗定时器 v1.0 | sgn_network_core | 超时判定 | 重复 HC16 定义 |

### 3.2 保留为独立扩展的原始文档

以下文档保留为 `sgn_extensions/` 下的独立文件，但需删除内部重复代码，改为引用核心文件：

| 扩展文档 | 保留理由 | 需删除的重复代码 | 需添加的引用 |
|---------|---------|----------------|-------------|
| 2-adic平方根 v1.1 | 数学预判储备 | Hensel 步数错误（修正为 45 步） | [sgn_math_core §2], [sgn_hc_ops §3] |
| Hensel提升求根 v1.0 | 多项式泛化 | 加法实现 | [sgn_hc_ops §3] |
| 补码负数扩展 v1.1 | 代数闭包 | 比较实现 | [sgn_hc_ops §2] |
| 整数次幂快速幂 v1.0 | 学习率衰减 | 乘法实现 | [sgn_hc_ops §3] |
| 投影除法沙盒 v1.1 | PC 端扩展 | 溢出语义 | [sgn_config_guide §2] |
| 超度量布朗运动 v1.0 | 远期时序 | 无 | [sgn_math_core §5] |
| 超度量球面反射 v1.0 | 数据增强 | 无 | [sgn_math_core §2] |
| Fréchet均值 v1.0 | 在线聚类 | 无 | [sgn_math_core §2] |
| 等距嵌入欧氏空间 v1.1 | 可视化 | 无 | [sgn_math_core §2] |
| 球面坐标与距离场 v1.0 | 可视化 | 无 | [sgn_math_core §2] |
| 计数布隆过滤器 v1.1 | 预筛选 | CBF 假阳性错误数据（修正） | [sgn_hc_ops §3], [sgn_config_guide §7] |
| 超度量重心 v1.0 | 聚类中心 | 无 | [sgn_math_core §2] |

---

## 4. 严重错误修正索引

| 编号 | 错误 | 原始文档 | 修正位置 | 修正内容 |
|------|------|---------|---------|---------|
| E1 | Hensel 步数=6（实际 45） | 2-adic平方根 v1.1 | sgn_extensions/2adic_sqrt.md | 修正为"45 步逐位提升，每 8 步填满 1 byte 层" |
| E2 | CBF 假阳性率 1.8%（实际 14.7%） | CBF v1.1 | sgn_extensions/bloom_filter.md | 按标准公式重新制表；推荐 m=4096 |
| E3 | float128 对 HC32"无损"（实际 15 位溢出） | 投影除法 v1.1 | sgn_extensions/projective_division.md | 修正为"默认 int_part=32 位时仍有 15 位损失" |
| E4 | 性能估算基于 24MHz/12T | 多份文档 | 全部核心文件 | 全部重新按 48MHz/1T 验算 |
| E5 | RS 查表法未说明 Flash 不可行 | RS v1.0 | sgn_storage_core §4.2 | 增加醒目警告：查表法仅 PC 验证器 |
| E6 | CBF"向 level 进位"与"仅 HC 模式"矛盾 | CBF v1.1 | sgn_extensions/bloom_filter.md | 明确 CBF 桶语义：仅 HC 模式 int_part 饱和 |

---

## 5. 性能基准总表（48MHz/1T 唯一权威值）

| 运算 | 机器周期 | 48MHz/1T 时间 | 旧估算(24MHz/12T) | 修正倍数 |
|------|---------|--------------|-------------------|---------|
| HC8 比较 | 12 | **0.25 μs** | 6.0 μs | 24× |
| HC8 加法 | 48 | **1.0 μs** | 24.0 μs | 24× |
| HC8 减法 | 48 | **1.0 μs** | 24.0 μs | 24× |
| 软阈值 HC8 | 48 | **1.0 μs** | 24.0 μs | 24× |
| Trie 单层查找 | 40 | **0.83 μs** | 20.0 μs | 24× |
| WTA 总耗时 | — | **16.7 μs** | ~400 μs | 24× |
| LRU 全局衰减(80) | 2400 | **50.0 μs** | 1.2 ms | 24× |
| Merkle 增量更新 | 400 | **8.3 μs** | 0.2 ms | 24× |
| TMR 投票 | 50 | **1.04 μs** | 25.0 μs | 24× |
| RS 解码纠正 | 150 | **3.1 μs** | 75.0 μs | 24× |
| 看门狗判定 | 12 | **0.25 μs** | 6.0 μs | 24× |
| 排序网络(N=256) | 24576 | **512 μs** | 12.3 ms | 24× |
| Hensel 全解(48位) | 4500 | **93.8 μs** | 2.25 ms | 24× |

**标准配置每步总耗时**: **~115 μs**（原估算 0.67 ms 严重过时）

---

## 6. 内存预算总表（34KB SRAM）

| 配置 | 组件 | 总占用 | 余量 | 可行性 |
|------|------|--------|------|--------|
| 裸核心 | 256N + 80模板 + Trie | 8,304 B | 22KB | ✅ |
| 标准推理 | + Merkle(80) + CBF256 + LRU | 10,952 B | 19KB | ✅ |
| 高可靠推理 | + TMR部分 + RS-LFSR + 校验和 | 12,176 B | 18KB | ✅ |
| 分布式节点 | + Lamport + Quorum + DFA(50) | 12,200 B | 18KB | ✅ |
| 极限挑战 | + 形态学 + 排序网络(N=128) | 19,500 B | 11KB | ✅ |
| **全量豪华** | + TMR全量 + CBF1024 + Merkle(256) | **30,784 B** | **超 64B** | ❌ |

---

## 7. 快速开始指南

### 7.1 新开发者路径

```

1. 阅读本文件（sgn_index_matrix.md）→ 了解体系架构
2. 阅读 sgn_math_core.md §1-2 → 理解超度量基础
3. 阅读 sgn_config_guide.md §1 → 选择数值模式
4. 根据场景查阅决策矩阵 §2 → 定位所需功能
5. 阅读对应核心文件 → 调用接口
6. 查阅原始文档映射 §3 → 了解历史演进

```

### 7.2 扩展开发者路径

```

1. 确认扩展功能不重复现有能力（查阅决策矩阵 §2）
2. 引用 sgn_math_core 的超度量定理，禁止重复证明
3. 引用 sgn_hc_ops 的基础运算，禁止重复实现
4. 引用 sgn_trie_core 的索引接口，禁止重复建树
5. 在 sgn_extensions/ 下编写业务逻辑文档
6. 提交前通过真值验证清单 §4 自检

```

---

*本文档为 SGN 技术栈的总索引入口。任何疑问应先查阅本索引，再定位到具体核心文件。*
```