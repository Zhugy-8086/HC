# SGN 配置与存储档位核心规范 (sgn_config_guide)

> **版本**: v2.0 (权威重构版)  
> **日期**: 2026-05-27  
> **性质**: SGN 技术栈唯一权威配置与存储档位源  
> **合并来源**: 《HC_level_separation_guide》《HC level_合体存储精度档位》《时间变量处理分工》  
> **核心原则**: 本文档统一定义三种数值模式、精度档位、模型文件格式、跨档位兼容协议。禁止其他文档重复定义溢出语义或存储格式。

---

## 1. 三种模式速查表（唯一权威）

| 模式 | 存储结构 | 比较规则 | 加法规则 | 线性映射 | 适用语义 |
|------|---------|---------|---------|---------|---------|
| **仅 HC** | `(int_part, frac[])` | 字典序（超度量） | 带进位，int_part 饱和/截断 | 局部可控（无撕裂） | 像素、速度、置信度、模板 |
| **仅 level** | `uint32_t level` | 自然整数序 | 普通整数加法 | 完全连续 | 阶段、计数、索引、轮次 |
| **合体** | `level × 256 + HC` | 先比 level 再字典序 | 完整进位（可向 level 溢出） | 不连续（进位撕裂） | 时间戳、累计量、大范围高精度存档 |

**一句话记忆**: HC 管精度与树索引，level 管范围与整数序，合体管极端场景。分离使用是默认姿态，合体是例外。

---

## 2. 溢出语义（唯一权威解释）

### 2.1 三种模式的溢出边界

| 模式 | 溢出者 | 被溢出者 | 处理策略 | 超度量结构 |
|------|--------|---------|---------|-----------|
| **仅 HC 饱和** | `int_part` 达到 255 | `frac` 层进位 | clamp 到 255 | **完整保留** |
| **仅 HC 回绕** | `int_part` 达到 255 | `frac` 层进位 | mod 256 | **完整保留** |
| **合体** | `int_part` 达到 255 | `level` 接收进位 | `level += 1` | **撕裂** |

### 2.2 安全舱壁原则（唯一权威表述）

> **仅 HC 模式的 255 上限是设计预期的安全舱壁**。其目的不是保精度，而是**保结构**——不让进位逃逸到 level 层撕裂超度量树。

> **合体模式在运算层是雷区，在存储层是保险箱**。运算禁止在合体上进行；存储允许合体仅执行带进位追加（时间戳、累计量）。

---

## 3. 精度档位表（唯一权威）

| 档位代号 | stored_level | 有效小数层 | 最小正数 | 十进制有效位 | 文件字节 | 典型场景 |
|---------|-------------|-----------|---------|-------------|---------|---------|
| **ARCHIVE** | 0 | 0（纯整数） | N/A | N/A | **13 B** | 版本号、索引、纯计数器 |
| **FAST** | 2 | 2 层 | 2⁻¹⁶ ≈ 1.53×10⁻⁵ | ~4.8 位 | **13 B** | 移动端缓存、低功耗预览 |
| **STD** | 4 | 4 层 | 2⁻³² ≈ 2.33×10⁻¹⁰ | ~9.6 位 | **13 B** | 标准模型、PC-MCU 互通 |
| **PREC** | 6 | 6 层 | 2⁻⁴⁸ ≈ 3.55×10⁻¹⁵ | ~14.4 位 | **13 B** | 高精度训练存档、科学计算 |
| **FUTURE** | 8 | 8 层（预留） | 2⁻⁶⁴ ≈ 5.42×10⁻²⁰ | ~19.2 位 | **15 B** | 未来 HC64/HC128 扩展 |

**关键设计**: 无论有效层数是 0 还是 6，文件字节长度**恒定**。无效层补零，保证 Flash 地址计算和 `fwrite` 的固定步长。

### 3.1 与编译期宏的映射

```cpp
// sgn_config.h
#define SGN_HPDC_LEVEL_FAST    2
#define SGN_HPDC_LEVEL_STD     4
#define SGN_HPDC_LEVEL_PREC    6
#define SGN_HPDC_LEVEL         SGN_HPDC_LEVEL_STD   // 默认标准模式

// 存储层档位自描述
#define SGN_STORE_LEVEL_ARCHIVE  0x00000000
#define SGN_STORE_LEVEL_FAST     0x00000002
#define SGN_STORE_LEVEL_STD      0x00000004
#define SGN_STORE_LEVEL_PREC     0x00000006
#define SGN_STORE_LEVEL_FUTURE   0x00000008
```

---

## 4. 模型文件格式（唯一权威）

### 4.1 单条合体记录（13 字节固定）

```
Offset  Size  Field           Description
─────────────────────────────────────────────────
0       1     mode            0x02 = 合体模式（存储层标识）
1       1     sign            0=正，1=负
2       1     int_part        整数部分 0~255
3       1     frac[0]         小数第 0 层（最高有效）
4       1     frac[1]         小数第 1 层
5       1     frac[2]         小数第 2 层
6       1     frac[3]         小数第 3 层
7       1     frac[4]         小数第 4 层
8       1     frac[5]         小数第 5 层（最低有效）
9       4     stored_level    uint32_t 小端，本记录的 level 值
                              （在标准档位文件中通常等于 0/2/4/6/8…）
─────────────────────────────────────────────────
Total: 13 字节（与有效层数无关）
```

**补零规则**（由文件头的 `GLOBAL_LEVEL` 决定，见 §4.2）：
- `GLOBAL_LEVEL=2`（FAST）: `frac[2..5]` 全部写 `0x00`
- `GLOBAL_LEVEL=4`（STD）: `frac[4..5]` 全部写 `0x00`
- `GLOBAL_LEVEL=6`（PREC）: `frac[0..5]` 全部有效
- `GLOBAL_LEVEL=0`（ARCHIVE）: `frac[0..5]` 全部写 `0x00`

> **⚠️ 职责澄清**
> 
> - `stored_level` 字段存储该记录的 `level` 值（即整数部分的 256 倍数），在标准档位文件中其值通常等于全局精度档位（0/2/4/6…）。  
> - **跨档位截断或扩展的决策依据是文件头中的 `GLOBAL_LEVEL` 与当前编译精度 `SGN_HPDC_LEVEL` 的比较，不是本记录的 `stored_level`。**  
> - 单条记录不提供完整的自描述信息。要正确解析 `frac` 的有效层数，必须先读取文件头的 `GLOBAL_LEVEL`。

### 4.2 模型文件头（23 字节全局自描述）

```
[MAGIC: 4B]      "SGN\x00"  文件签名
[VERSION: 1B]    0x01        格式版本
[GLOBAL_LEVEL: 4B]  uint32_t  本文件全局精度档位
[NUM_RECORDS: 4B]  uint32_t  记录总数 N
[MERKLE_ROOT: 8B]  HC Merkle 根哈希（完整性）
[RS_PARITY: 2B]   Reed-Solomon 校验（纠错）
─────────────────────────────────────
Header: 23 字节
Followed by N × 13B records...
```

**自描述语义**:
- 读取文件时，先读 `GLOBAL_LEVEL`，与当前编译的 `SGN_HPDC_LEVEL` 比对
- 若 `GLOBAL_LEVEL > SGN_HPDC_LEVEL`: 文件精度高于当前能力，**截断降级**
- 若 `GLOBAL_LEVEL < SGN_HPDC_LEVEL`: 文件精度低于当前能力，**补零扩展**
- 若相等: 直接映射，零开销

> **注意**：`stored_level` 字段（见 §4.1）不参与跨档位兼容决策。所有截断/扩展行为仅由文件头的 `GLOBAL_LEVEL` 与当前编译的 `SGN_HPDC_LEVEL` 决定。

---

## 5. 跨档位兼容协议（唯一权威）

> **脚注**：本节中的「文件 level」均指文件头（§4.2）中的 `GLOBAL_LEVEL`，与单条记录内的 `stored_level` 无关。

### 5.1 补零扩展（文件 level < 当前编译 level）

```
文件: stored_level=4, frac=[a,b,c,d,0,0]
当前: SGN_HPDC_LEVEL=6

加载后:
  frac[0..3] = a,b,c,d（原样保留）
  frac[4..5] = 0,0（补零）
  物理值不变，精度提升（新增层为零，不影响比较结果）
```

**数学保证**: 补零扩展不改变原有物理值，新增层贡献为零。

### 5.2 截断降级（文件 level > 当前编译 level）

```
文件: stored_level=6, frac=[a,b,c,d,e,f]
当前: SGN_HPDC_LEVEL=4

加载后:
  frac[0..3] = a,b,c,d（保留高位）
  frac[4..5] 丢弃（精度损失可控）
  物理值误差 < 2⁻³²（对于 STD 档，误差在 10⁻¹⁰ 量级）
```

**数学保证**: 截断只是丢弃低位，不修改高位，数量级不变，**永不溢出**。

### 5.3 与浮点转换的对比

| 转换方向 | 浮点体系 | HC 合体存储 |
|---------|---------|------------|
| float64 → float16 | 需重新量化、可能溢出 inf | 截断降级：安全，无溢出 |
| float16 → float64 | 直接补零扩展，但精度不真提升 | 补零扩展：语义一致 |
| 跨平台一致性 | 依赖 FPU 实现，可能差几个 bit | **逐字节相同，md5sum 一致** |

---

## 6. 时间变量处理（唯一权威分工）

### 6.1 时间戳用 HC 单独，不用合体

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 记录单次判断的时间戳 | **HC16 单独** | 范围 65535 秒 ≈ 18.2 小时，精度 15 μs |
| 记录两个判断的时间差 | **HC16 单独**（减法结果） | 时间差在秒~毫秒范围 |
| 周期性事件 | **HC16 单独** + 阈值比较 | 加法累加时间，比较与阈值 |
| 系统运行总时长 | **uint64_t 毫秒** 或 RTC | 不要用 HC，这是外部职责 |
| 大范围+高精度单一数值 | **HC+level 合体** | 这才是合体的设计目标 |

**关键约束**: 神经元的时间戳字段严禁使用合体。level 每增加 1 跳跃 256 秒，对单次判断无意义且危险。

### 6.2 一般数值计算推广

```
该数值是否需要超过 255 秒（或 255 个单位）的范围？
├─ 否 → 使用仅 HC 模式（HC8/HC16）
│       └─ 是否需要超度量比较或 Trie 索引？
│           ├─ 是 → 仅 HC（模板匹配、像素值、响应速度）
│           └─ 否 → 仍可用仅 HC（简单范围 [0, 256)）
└─ 是 → 是否需要微秒级以上精度？
    ├─ 否 → 使用仅 level 模式（uint32_t 整数秒）
    └─ 是 → 使用 HC+level 合体（大范围 + 高精度）
```

**一句话推广版**: 凡是局部特征、模板、强度、速度，用仅 HC；凡是阶段、计数、索引、调度，用仅 level；只有时间戳、累计量、大跨度权重，才动用合体。

---

## 7. 内存互斥规则（来自 34KB SRAM 验算）

```cpp
// sgn_memory_budget.h —— 必须遵守的互斥宏

#define SGN_SRAM_TOTAL     34816
#define SGN_SRAM_RESERVED  4096    // 栈(2KB) + 中断(1KB) + 全局(1KB)
#define SGN_SRAM_BUDGET    30720

// 互斥宏：以下组合不能同时启用
#if defined(SGN_TMR_FULL) && defined(SGN_CBF_1024)
    #error "TMR_FULL 与 CBF_1024 互斥：合计 > 16KB"
#endif

#if defined(SGN_RS_TABLE_MODE)
    #error "RS_TABLE_MODE 需要 64KB Flash，AI8051U 仅 64KB 总 Flash，与程序代码互斥"
#endif

#if defined(SGN_TMR_FULL) && defined(SGN_FRECHET_16BIT)
    #error "TMR_FULL 与 FRECHET_16BIT 互斥：合计接近 SRAM 上限"
#endif

#if defined(SGN_SORTNET_256) && defined(SGN_TMR_FULL)
    #error "SORTNET_256 需要 2KB 缓冲，与 TMR_FULL 互斥"
#endif

// 动态降级阈值
#define SGN_TEMPLATE_THRESHOLD_SCALE    128
#if SGN_MAX_TEMPLATES > SGN_TEMPLATE_THRESHOLD_SCALE
    #warning "模板数 > 128，自动关闭 FRECHET 频率表以节省 3.5KB"
    #undef SGN_FRECHET_ENABLE
#endif
```

### 7.1 配置组合可行性矩阵

| 配置名称 | 总占用 | vs 30KB | 可行性 |
|---------|--------|---------|--------|
| 裸核心 | 8,304 B | 余 22KB | ✅ 安全 |
| 标准推理 | 10,952 B | 余 19KB | ✅ 安全 |
| 高可靠推理 | 12,176 B | 余 18KB | ✅ 安全 |
| 分布式节点 | 12,200 B | 余 18KB | ✅ 安全 |
| 全索引 | 14,800 B | 余 16KB | ✅ 安全 |
| 极限挑战 | 19,500 B | 余 11KB | ✅ 可接受 |
| **全量豪华** | **30,784 B** | **超 64 B** | ❌ **不可行** |

---

## 8. 引用规范

任何 SGN 文档在涉及以下概念时，**必须**引用本文档：

- 三种模式选择 → **§1 速查表**
- 溢出语义解释 → **§2**
- 精度档位定义 → **§3**
- 模型文件格式 → **§4**
- 跨档位兼容 → **§5**
- 时间变量分工 → **§6**
- 内存互斥规则 → **§7**

---

*本文档为 SGN 技术栈的配置与存储层 ABI 契约。所有模块必须遵守上述模式定义、档位表和内存互斥规则。*
